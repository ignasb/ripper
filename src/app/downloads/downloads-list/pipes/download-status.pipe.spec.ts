import { DownloadStatusPipe } from './download-status.pipe';

describe('DownloadStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new DownloadStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
